server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/capstone?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345

  spring:
    jpa:
      hibernate:
        ddl-auto: create  # create / update / validate / none 가능
      show-sql: true
      properties:
        hibernate:
          format_sql: true